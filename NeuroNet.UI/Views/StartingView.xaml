<UserControl x:Class="NeuroNet.UI.Views.StartingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Button Command="{Binding LearnNetCommand}" Content="Обучить сеть"
                    Margin="10" Width="200" Height="40"/>
            <Button Command="{Binding LoadNetCommand}" Content="Загрузить обученную сеть"
                    Margin="10" Width="200" Height="40"/>
        </StackPanel>
    </Grid>
</UserControl>
